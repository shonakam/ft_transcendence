{
  "name": "pong-tournament",
  "version": "1.0.0",
  "description": "TypeScript Pong Game for ft_transcendence",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp srcs/pong/pong_page.html dist/index.html",
    "dev": "tsc --watch",
    "clean": "rm -rf dist/",
    "rebuild": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["pong", "typescript", "game", "42school"],
  "author": "mfukui",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
